spring.application.name={{ project_name }}
server.port=8080

# Virtual threads: lightweight concurrency for request handling (JDK 21+).
# Recommended on JDK 24/25 to reduce platform-thread overhead under load.
spring.threads.virtual.enabled=true

# Logging
logging.level.root=INFO
logging.level.{{ group }}=DEBUG
